export type RootStackParamList = {
    login: undefined; // A tela de login não recebe parâmetros
    register: undefined; // A tela de cadastro não recebe parâmetros
    // Adicione outras telas conforme necessário
  };